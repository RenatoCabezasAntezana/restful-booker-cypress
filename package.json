{
  "name": "restfull-booker-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "cypress:execution-tags": "cypress run --browser chrome --headless"
},
  "keywords": [
    "cypress"
  ],
  "author": "Renato Cabezas",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^14.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.5",
    "cypress": "^11.2.0",
    "esbuild": "^0.27.3"
  },
  "cypress-cucumber-preprocessor": {
    "json": {
      "enabled": true,
      "output": "cypress/reports/cucumber-results.json",
      "formatter": "./cucumber-json-formatter"
    },
    "messages": {
      "enabled": false
    },
    "stepDefinitions": [
      "cypress/e2e/steps-definitions/*.{js, ts}"
    ]
  }
}
